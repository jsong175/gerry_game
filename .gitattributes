# Normalize all text to LF in the repo so generated JSON is byte-identical
# across Windows (local) and Linux (CI), keeping the reproducibility gate green.
* text=auto eol=lf
*.png binary
*.jpg binary
