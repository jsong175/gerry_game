[tool.pytest.ini_options]
testpaths = ["engine/tests"]
addopts = "--cov=engine --cov-report=term-missing --cov-fail-under=80"

[tool.coverage.run]
omit = ["engine/tests/*", "engine/export_fixtures.py"]
